[pytest]
minversion = 7.0
addopts = -ra --strict-markers
pythonpath = .
testpaths =
    tests
markers =
    unit: Unit tests
    integration: Integration tests
    e2e: End-to-end tests
    slow: Slow running tests
